```python
import numpy as np

def priority_v2(k: int, n: int) -> float:
  """Improved version of `priority_v1`."""
  # Prioritize larger numbers, numbers close to sqrt(n), and numbers with fewer prime factors
  return k / n + (1 / abs(k - np.sqrt(n))) + (1 / len(set(funsearch.prime_factors(k))))
```